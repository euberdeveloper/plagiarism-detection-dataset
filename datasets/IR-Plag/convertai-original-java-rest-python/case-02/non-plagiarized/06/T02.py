import math

radius = float(input("Enter the radius and the length of the cilinder : "))
length = float(input())

area = math.pi * radius ** 2
volume = math.pi * radius ** 2 * length

print(f"The area is {area}")
print(f"The volume is {volume}")

